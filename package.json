{
  "dependencies": {
    "axios": "^1.9.0",
    "dotenv": "^16.4.7",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  },
  "name": "igdtuw_marketplace",
  "version": "1.0.0",
  "description": "Full-stack app with React frontend and Node backend.",
  "main": "api.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start-frontend && npm run start --prefix backend",
    "start-frontend": "npm run build --prefix frontend && npm run start --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
